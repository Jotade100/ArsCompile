class PrimeByArithmetic {
	private static void printPrime(int i, int p) {

		IO.putString("Prime[");

		IO.putInt(i); //inválido

		IO.putString("] = ");

		IO.putInt(p);

		IO.putString("\n");

	}
}