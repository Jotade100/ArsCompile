class Program {
        int x; int y;
                void main() {
                    int z;
                    x = read();
                    y = read();
                    print(gcd(x,y));
                }
                int gcd(int a, int b) {
                    if ( b==0 ){
                        return a;
                        
                    } else {
                        return gcd(b, a % b);}
                }
            }
